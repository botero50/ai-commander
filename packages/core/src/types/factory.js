/**
 * Check if a value is a Factory.
 */
export function isFactory(value) {
    return typeof value === 'function';
}
/**
 * Check if a value is an AsyncFactory.
 */
export function isAsyncFactory(value) {
    return typeof value === 'function';
}
//# sourceMappingURL=factory.js.map