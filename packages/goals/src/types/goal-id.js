/**
 * Create a typed GoalId from a string.
 */
export function createGoalId(id) {
    return id;
}
/**
 * Validate a string as a GoalId.
 */
export function isGoalId(value) {
    return typeof value === 'string' && value.length > 0;
}
//# sourceMappingURL=goal-id.js.map