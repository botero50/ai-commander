/**
 * Create a typed PlanId from a string.
 */
export function createPlanId(id) {
    return id;
}
/**
 * Validate a string as a PlanId.
 */
export function isPlanId(value) {
    return typeof value === 'string' && value.length > 0;
}
//# sourceMappingURL=plan-id.js.map